from setuptools import setup

if __name__ == "__main__":
    setup(
	# TODO: Any packages required at project runtime that are to be installed using the pip pkg mgr need to be listed here:
        install_requires=[
	]
    )
